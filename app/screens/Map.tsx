import { StyleSheet, Text, View } from "react-native";

const Map: React.FC = () => {
	return (
		<View>
			<Text>Map</Text>
		</View>
	);
};

export default Map;

interface Styles {}

const styles = StyleSheet.create<Styles>({});
